import { CustomPasswordValidator } from './custom-password-validator';

describe('CustomPasswordValidator', () => {
  it('should create an instance', () => {
    expect(new CustomPasswordValidator()).toBeTruthy();
  });
});
